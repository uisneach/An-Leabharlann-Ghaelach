// Footer.tsx
import React from 'react';

const Footer: React.FC = () => {
  return (
    <footer>
      {/* No explicit footer in original HTML, so empty or add if needed */}
    </footer>
  );
};

export default Footer;